#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../../@electric-sql/pglite-socket/dist/scripts/server.js" "$@"
else
  exec node  "$basedir/../../../../@electric-sql/pglite-socket/dist/scripts/server.js" "$@"
fi
# cmd-shim-target=D:/kuji_system/apps/backend/node_modules/.pnpm/@prisma+dev@0.24.3_typescript@6.0.3/node_modules/@electric-sql/pglite-socket/dist/scripts/server.js
